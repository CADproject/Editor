<Window x:Class="CADView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CADView"
        mc:Ignorable="d"
        AllowsTransparency="False"
        Title="Editor" BorderThickness="2" Height="450" Width="870" MinHeight="450" MinWidth="870" WindowStartupLocation="CenterScreen"
        d:DataContext="{d:DesignInstance Type=local:MainWindowViewModel, IsDesignTimeCreatable=True}"
        >
    <Window.BorderBrush>
        <LinearGradientBrush EndPoint="0.5,1" MappingMode="RelativeToBoundingBox" StartPoint="0.5,0">
            <GradientStop Color="Black" Offset="1"/>
            <GradientStop Color="White" Offset="0.495"/>
            <GradientStop Color="#FF5A5A5A"/>
            <GradientStop Color="#FF676767" Offset="0.998"/>
        </LinearGradientBrush>
    </Window.BorderBrush>
    <Grid>
        <Grid.Resources>
            
            <DataTemplate DataType="{x:Type local:MenuSubItem}">
                <!--<Border BorderBrush="Black" BorderThickness="1">-->
                    <Expander Width="{Binding Width}" MinHeight="{Binding Height}" Background="{Binding Color}" Margin="0,0,0,0">
                        <Expander.Header>
                            <BulletDecorator Margin="0,8,0,0" MinHeight="26">
                                <BulletDecorator.Bullet>
                                <Image Source="{Binding Image}" Stretch="Fill" Width="25" Height="25" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </BulletDecorator.Bullet>
                                <!--<TextBlock Margin="3,0,0,0" Text="{Binding HintText}" VerticalAlignment="Center" HorizontalAlignment="Stretch" Foreground="White" />-->
                            </BulletDecorator>
                        </Expander.Header>
                        <WrapPanel>
                            <ItemsControl Margin="0,5,0,0" ItemsSource="{Binding SubItems}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Vertical"></StackPanel>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </WrapPanel>
                    </Expander>
                <!--</Border>-->
            </DataTemplate>
            
            <DataTemplate DataType="{x:Type local:MenuButtonItem}">
                <Label Width="{Binding Width}" Height="{Binding Height}" Background="#80808080"
                       HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                       VerticalAlignment="Top" Margin="1,1,1,1" Foreground="White" ToolTip="{Binding HintText}">
                    <Label.InputBindings>
                        <MouseBinding Command="{Binding DataContext.ControllerDialogCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Window}}"
                                      CommandParameter="{Binding OperationType}" MouseAction="LeftClick" />
                    </Label.InputBindings>
                    <Label.Style>
                        <Style TargetType="Label">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True" >
                                    <Setter Property="BorderBrush" Value="Black"/>
                                    <Setter Property="BorderThickness" Value="1"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Label.Style>
                    <Image Margin="1,1,1,1" Source="{Binding Image}" Stretch="Fill" Width="38" Height="38"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
                    <!--<DockPanel>
                        <Label Margin="1" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="0" Foreground="White" Content="{Binding HintText}" />
                    </DockPanel>-->
                </Label>
            </DataTemplate>
            
            <DataTemplate DataType="{x:Type local:MenuExpanderItem}">
                <Expander Background="{Binding Color}">
                    <Expander.Header>
                        <BulletDecorator Margin="0,0,20,0">
                            <BulletDecorator.Bullet>
                                <Image Source="{Binding Image}" Stretch="Fill" Width="64" Height="64" HorizontalAlignment="Left" VerticalAlignment="Top" />
                            </BulletDecorator.Bullet>
                            <TextBlock Margin="10,0,0,0" Text="{Binding HintText}" VerticalAlignment="Center" HorizontalAlignment="Stretch" Foreground="White" />
                        </BulletDecorator>
                    </Expander.Header>
                    <WrapPanel>
                        <ItemsControl ItemsSource="{Binding SubItems}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"></StackPanel>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </WrapPanel>
                </Expander>
            </DataTemplate>
            
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="50" Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <!--<GridSplitter Grid.Row="0" ResizeBehavior="BasedOnAlignment" ResizeDirection="Rows" HorizontalAlignment="Stretch" Background="Black" VerticalAlignment="Bottom" Height="2" Margin="0,0,0,0"/>-->
        <Grid Grid.Row="1">
            <Grid.Resources>
                <ControlTemplate x:Key="CustomHeaderTemplate">
                    <Border BorderThickness="2" CornerRadius="4">
                        <DockPanel LastChildFill="True">
                            <Button Content="X" DockPanel.Dock="Right" 
                                    IsEnabled="{Binding DataContext.IsActive, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                    Command="{Binding DataContext.CloseDocumentCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                    CommandParameter="{Binding}" Style="{StaticResource RoundedButton}" FontWeight="Bold" Width="20" Height="20">
                            </Button>
                            <Label Content="{Binding}" />
                        </DockPanel>
                    </Border>
                </ControlTemplate>
            </Grid.Resources>
            <TabControl ItemsSource="{Binding DocumentViewModelsTabs, Mode=TwoWay}"
                        SelectedIndex="{Binding SelectedDocumentIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <TabControl.ItemContainerStyle>
                    <Style TargetType="TabItem">
                        <Setter Property="Header" Value="{Binding Title}" />
                        <Setter Property="HeaderTemplate">
                            <Setter.Value>
                                <DataTemplate>
                                    <Border BorderThickness="2" CornerRadius="4">
                                        <Control Template="{StaticResource CustomHeaderTemplate}"></Control>
                                    </Border>
                                </DataTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TabControl.ItemContainerStyle>
            </TabControl>
        </Grid>
        <Grid Row="0">
            <ListBox ScrollViewer.VerticalScrollBarVisibility="Auto" ItemsSource="{Binding UIMenuElements}">
                <ListBox.Resources>
                    <!--<Style TargetType="{x:Type Expander}">
                        <Setter Property="IsExpanded">
                            <Setter.Value>
                                <Binding Path="IsSelected" RelativeSource="{RelativeSource AncestorType={x:Type ListBoxItem}}" />
                            </Setter.Value>
                        </Setter>
                    </Style>-->
                </ListBox.Resources>
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <DockPanel ></DockPanel>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.Template>
                    <ControlTemplate TargetType="{x:Type ListBox}">
                        <ItemsPresenter/>
                    </ControlTemplate>
                </ListBox.Template>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <ContentPresenter Content="{TemplateBinding Content}"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Grid>
    </Grid>
</Window>
